apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: annotation-operator
rules:
- apiGroups:
  - sgerard.be
  resources:
  - annotations
  verbs:
  - list
  - watch
  - get
  - update
  - patch
- apiGroups: [""]
  resources:
    - endpoints
  verbs:
    - list
    - watch
    - get
    - create
    - update

# kubectl auth can-i --as=system:serviceaccount:default:annotation-operator --list